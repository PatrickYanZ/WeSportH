{
  "name": "wesporth-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply wesporth-db --local",
    "db:remote": "wrangler d1 migrations apply wesporth-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
